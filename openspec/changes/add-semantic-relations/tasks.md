# Tasks: Add Semantic Relations

- [ ] Document standard relation types in `backend/models.py`
- [ ] Add helper endpoints for common relations (e.g., `POST /api/concepts/{id}/requires` to create REQUIRES relation)
- [ ] Update UI with relation creation forms
- [ ] Add strength and rationale fields to relation forms
- [ ] Test creating various relation types via UI
